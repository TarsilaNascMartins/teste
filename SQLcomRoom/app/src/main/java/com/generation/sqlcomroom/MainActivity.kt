package com.generation.sqlcomroom

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        /* Estrutura Room

        Banco de dados  - Usuário

        DAO - Data Acess Object - O cara que cudiara das quarys

        Querys SQL  - Select, Insert, Updata, Delete

        Database - Instancia do banco de daods

        Repository - Ponte entre o DAO e a instancia do Data base
        ViewModel -

         */
    }
}